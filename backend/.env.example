# Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Local PostgreSQL:
DATABASE_URL=postgresql://postgres:password@localhost:5432/parapharmacie_db

# Railway PostgreSQL (auto-set by Railway):
# DATABASE_URL=postgresql://postgres:xxxxx@containers-us-west-xxx.railway.app:5432/railway

# JWT Secret
# IMPORTANT: Generate a secure secret for production!
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend URL (for CORS)
# Local development:
FRONTEND_URL=http://localhost:3001

# Production (update with your actual frontend URL):
# FRONTEND_URL=https://yourdomain.com

# Token Expiration
JWT_EXPIRES_IN=7d
