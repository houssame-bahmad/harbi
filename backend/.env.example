# Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration - OPTION 1: Individual Variables (Recommended for Hostinger)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=parapharmacie_db

# Database Configuration - OPTION 2: Connection URL (Railway auto-sets this)
# DATABASE_URL=mysql://root:password@localhost:3306/database_name

# CORS Configuration
CORS_ORIGIN=http://localhost:3001
# Production: CORS_ORIGIN=https://harbi.benmina.com

# JWT Configuration
# IMPORTANT: Generate a secure secret for production!
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Rate Limiting (optional - defaults provided)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Legacy support (CORS_ORIGIN takes precedence)
# FRONTEND_URL=http://localhost:3001
